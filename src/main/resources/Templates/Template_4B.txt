# TEMPLATE_4B -- Busca a quantidade de negócios para uma ou mais empresas com nome similar.
SELECT ?S1 ?ANS WHERE {
    ?S1 P7 ?label .
    # Filtro case-insensitive para o nome da empresa
    FILTER(REGEX(STR(?label), "#ENTIDADE_NOME#", "i"))
    
    ?S1 P1 ?SO1 .
    ?SO1 P3 ?SO2 .
    ?SO2 P5 ?S2 .
    ?S2 P6 "#DATA#"^^xsd:date .
    ?SO2 #VALOR_DESEJADO# ?ANS .
}